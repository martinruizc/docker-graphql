name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v4.0.3
      with:
        major_pattern: "major:"
        minor_pattern: "feat:"
        format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version


    - name: Docker Login
      env:
        DOCKER_USER: ${{secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "New version: $NEW_VERSION!!!!!"

<<<<<<< HEAD
    - name: Build Docker Image
      env: 
        NEW_VERSION: ${{ steps.vesion.outputs.version }}
      run: |
        docker build -t martinruizcl/docker-graphql:$NEW_VERSION .

    - name: Push Docker Image
      env:
        NEW_VERSION: ${{ steps.vesion.outputs.version }}
      run: |
        docker push martinruizcl/docker-graphql:$NEW_VERSION
=======
    # - name: Build Docker Image
    #   run: |
    #     docker build -t martinruizcl/docker-graphql:0.0.2 .

    # - name: Push Docker Image
    #   run: |
    #     docker push martinruizcl/docker-graphql:0.0.2
>>>>>>> 7cf4b7cb6b0c905f63786b4b9c9ac6e92873a378

    


    # - name: Build the Docker image
    #   run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
